echo "--->清除垃圾文件"
hexo clean

echo "--->部署"
hexo g

echo "--->提交到hexo分支"
git add .
git commit -m "update hexo"
git push origin hexo

echo "--->提交到master分支"
cd public
git add .
git commit -m "update master"
git push origin master

cd ../

echo "--->完成"